//= require youstars

angular.module('youstars').config(function ($routeProvider, $locationProvider) {
	$routeProvider.when('', {
		templateUrl: '<%= asset_path("index.html") %>',
    controller: 'IndexController'
	})
});
